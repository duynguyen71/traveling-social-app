import 'package:flutter/cupertino.dart';

class CommentEntryStaless extends StatelessWidget {
  const CommentEntryStaless({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
